import { ColorPickerDirective } from './color-picker.directive'

describe('ColorPickerDirective', () => {
    it('should create an instance', () => {
        const directive = new ColorPickerDirective()
        expect(directive).toBeTruthy()
    })
})
