import { MetaDirective } from './meta.directive'

describe('MetaDirective', () => {
    it('should create an instance', () => {
        const directive = new MetaDirective()
        expect(directive).toBeTruthy()
    })
})
